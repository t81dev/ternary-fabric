# --- Compiled Binaries & Objects ---
bin/*
benchmarks/synthetic/saturation
benchmarks/synthetic/density
benchmarks/gemm/gemm_bench
benchmarks/lstm/lstm_bench
benchmarks/temporal/lstm_persistent
obj_dir/*
*.o
*.a
*.d
*.so                # careful: this catches pytfmbs/*.so too — see exclusion below
v_bench_binary
v_profile_binary

# --- Verilator & Simulation Artifacts (this is the key part) ---
obj_dir/             # catches EVERYTHING inside, including all V*.cpp, V*.h
*.vcd
*.fst
*.lxt2
*.fsdb
*.log                # Verilator/sim logs
V*.mk                # generated makefiles (Vtop.mk etc.)
V*__*.cpp            # partitioned DepSet files, Trace, Slow, etc.
V*__*.h
V*__Syms*
V*___024*
V*__pch.h
V*__ver.*
*.cpp                # broad catch for generated .cpp OUTSIDE obj_dir (rare, but safe)
*.h                  # same for .h — but only if not in source dirs

# If any generated files escape obj_dir (uncommon), but to be safe:
**/V*.cpp
**/V*.h

# --- Python & Build System ---
__pycache__/
*.pyc
.pytest_cache/
build/
build.old/
dist/
*.egg-info/
venv/
.env

# --- Ternary Data & Logs ---
*.tfrm
*.tfd
*.csv
weights.txt
trace.vcd
waveform.vcd

# --- Synthesis / FPGA ---
*.bit
*.rpt
*.dcp
vivado*.log
*.jou
*.xpr
*.runs/
*.cache/

# --- OS / Editor ---
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# --- Exceptions: Don't ignore your own source .so or key files ---
# !src/pytfmbs/*.so     # DO NOT keep the Python extension (build from source)
!tests/*.cpp          # your hand-written testbench .cpp
!tests/*.h            # if you add any
!src/libtfmbs_*.c     # your interposer libs (even if .c, for consistency)
